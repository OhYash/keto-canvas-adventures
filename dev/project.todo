# OhYa.sh Portfolio — Development Tracker
Last updated: 2026-08-11

This is the ONLY place for work items (see CLAUDE.md & GEMINI.md). Sections: DONE, PENDING,
NEEDS INPUT, LEARNINGS, PLANS. Mark completed items [x] and move them to DONE.
Content critiques and research live in dev/research/, referenced from items here.

---

## DONE

### Infrastructure & Code
- [x] Infinite canvas system — InfiniteCanvas + useViewport / useSectionManagement /
      useCanvasEvents / useGridNavigation hook split
- [x] GitHub Pages deploy on push to main (.github/workflows/deploy.yml); surge preview via `make surge`
- [x] Visitor tracking — useVisitTracking hook, localStorage-backed
- [x] Canvas borders / always able to return home (2025-06-28)
- [x] Prevent canvas movement while scrolling on phone (2025-06-28)
- [x] Contact page with email protection, social handles, and Telegram link
- [x] Now section content-as-data — src/data/nowData.json + HOW_TO_UPDATE_NOW.md
- [x] Page title ("OhYa.sh – Everything Yash. Filtered.")
- [x] Order INR Finance Compass first in ProjectsSection projects list (2026-07-10)
- [x] Resume variants in repo; download links point to s2 variant
- [x] Initialize Antigravity support — GEMINI.md, .gemini/rules.md, .gemini/skills/verify/SKILL.md configured for AGY / Antigravity 2.0 (2026-08-07)
- [x] Native Writing & Essays blog section shipped (2026-08-11) — registered as `writing` at (2000, 0) under Work; src/data/blogData.ts + WritingSection.tsx + ArticleReaderView.tsx overlay with video hero player support and distraction-free typography.


### Content & Voice (from the 2025-05-23 voice audit — dev/research/)
- [x] Travel Stories shipped — "not available" toast removed, section reachable
- [x] Boilerplate purge — "Always X · Always Y" badges and fortune-cookie quotes
      removed (Keto's quote intentionally kept)
- [x] Contact section stock photo / "Ready to connect!" removed
- [x] Welcoming text refactor on home (commit 978c737)
- [x] Home headline reframed to founder-forward voice (2026-07-12): "I'm Yash.
      Engineer today, founder in progress." — earned by the Projects card below it
- [x] Ataco page shipped (2026-07-08) — subsection behind Keto at (0, -2000),
      content in src/data/atacoData.ts from dev/research/ataco-page-content.md,
      photo placeholders until real pics arrive; plan: dev/plans/ataco-page.md
- [x] Pulled content from LinkedIn Profile PDF (2026-07-10): rewrote INR Finance
      Compass project blurb with the AI-native "financial context" narrative
      (ProjectsSection.tsx); added Languages card (Hindi/English/German) to
      PersonalSection "About Me" grid
- [x] Now section refresh (2026-07-12): new Location/Travel/Finance Compass/Work/
      Riding/Routine content; added internal-nav support to NowSection (a plan item
      can carry `internalTarget` to pan to another section instead of an external
      link) — SectionRenderer now passes onNavigateToSection into NowSection
- [x] Now & Work sections status update (2026-08-07): updated nowData.json,
      HomeSection, and WorkSection to reflect transition from TestGorilla to actively
      looking for full-time senior backend roles and freelance clients (with internalTarget navigation to Contact)
- [x] Work & Now section dates & plans update (2026-08-07): updated TestGorilla contract end date to August 2026 in WorkSection.tsx; removed Europe trip mentions in nowData.json and updated focus to landing next role before bigger plans.

---

## PENDING

### Content
- [ ] Replace Unsplash stock photos with real Keto pics — KetoSection.tsx and
      DetailedStoryView.tsx still use unsplash URLs; biggest credibility leak on the site
- [ ] Rewrite travel stories in DetailedStoryView.tsx with real first-person memories
      (one specific thing, one weird thing, one thing that went wrong) — or cut
      destinations not actually visited (see NEEDS INPUT)
- [ ] Add real projects to ProjectsSection
- [ ] Update personal profile content
- [ ] Update work experience (current and past roles)
- [ ] Swap Ataco photo placeholders for real pics — owner is supplying photos;
      captions/slots live in src/data/atacoData.ts (atacoPhotoSlots)

### Code
- [ ] Page-flow tracking — which direction visitors pan/navigate; integration point is
      updateCurrentSection in useSectionManagement (see LEARNINGS)
- [ ] Subsection discoverability — Travel (under Personal), Projects (under Hobbies) are
      invisible until you're inside the parent; add "deeper ↓ / more →" micro-hints on
      parent cards
- [ ] Surface the good subtitles from useSectionManagement's sections array onto the
      homepage cards (voice exists in the data layer, hidden from the home screen)
- [ ] Add chain in each direction — go deeper than one subsection level
- [ ] Extract remaining hard-coded content (work, personal, projects) into src/data/
      like nowData.json — prerequisite for easy content updates
- [ ] Quality gate — add `npx tsc --noEmit` + lint as pre-commit habit; no test setup
      exists yet (vitest later, if ever)
### SEO & Indexability (from dev/research/seo-analysis.md)
- [ ] Fix GitHub Pages HTTP 404 SPA fallback — add static site pre-rendering (`vite-plugin-prerender` or build-time HTML generator) so `/work`, `/personal`, `/projects`, `/keto`, `/now`, `/contact` return `HTTP 200 OK` static HTML instead of 404.
- [ ] Implement dynamic head tags (`react-helmet-async`) — update `<title>`, `<meta name="description">`, `<meta property="og:title">`, `<meta property="og:description">`, `<meta property="og:url">`, and `<link rel="canonical">` dynamically on section changes.
- [ ] Convert canvas navigation buttons to semantic anchor tags (`<a href="...">`) — replace internal `<button onClick>` calls with `<a href="...">` tags so search crawlers can discover sub-routes through the DOM link graph.
- [ ] Create social sharing card (`public/og-image.png`) — design and save 1200x630px image at `public/og-image.png` (currently returning 404 Not Found on social shares).
- [ ] Generate `public/sitemap.xml` & update `public/robots.txt` — generate XML sitemap listing all section routes and append `Sitemap: https://ohya.sh/sitemap.xml` to robots.txt.
- [ ] Fix heading hierarchy (`<h1>` tag duplication) — refactor `SectionRenderer.tsx` and section components so that only the active view renders an `<h1>` element, eliminating 10 concurrent `<h1>` tags on the DOM tree.
- [ ] Embed Schema.org JSON-LD structured data — add `Person`, `ProfilePage`, `BreadcrumbList`, and `CreativeWork` JSON-LD scripts for Google Knowledge Panel and Rich Results.
- [ ] Image optimization & Core Web Vitals (CLS) — add explicit `width` and `height` attributes to profile and story images, convert static assets to WebP/AVIF, and enable native `loading="lazy"`.
- [ ] Refactor ContactSection.tsx (~217 lines) into smaller components
- [ ] Split useSectionManagement.ts (~230 lines) into focused hooks
- [ ] Add error boundaries around section rendering

---

## NEEDS INPUT
- Travel stories — which destinations have you actually visited? Needed to decide
  rewrite vs. cut per story.
- "Recently" rule — Now items older than ~2 months should drop the word "recently";
  adopt as a content rule?

---

## LEARNINGS
- Touch events conflict with browser navigation/scroll on mobile — canvas panning must
  stand down while the user is scrolling; be careful with preventDefault in useCanvasEvents.
- All navigation flows through updateCurrentSection in useSectionManagement — the single
  integration point for any tracking or navigation feature.
- Voice is the product: specific first-person copy ("Tall skinny fat, slight athletic",
  the Now section) is what works; template phrasing reads as Lovable boilerplate.
  Full critiques: dev/research/23May26_claude_idea.md and 23May26_gemini_ideas.txt.
- Axis mental model for future sections: professional content on the horizontal axis,
  personal on the vertical. Don't rearrange existing sections; apply when adding new ones.
- Adding a section takes FOUR registration points: useSectionManagement's array,
  an App.tsx route above the catch-all, InfiniteCanvas's validSections list, and
  NavigationBreadcrumb's sectionNames map. Missing the route or validSections fails
  silently at build time and 404s at runtime — verify in a browser
  (.claude/skills/verify/SKILL.md has the recipe).

---

## PLANS
None yet. Create dev/plans/<slug>.md before non-trivial multi-step work
(template in dev/Agents.md) and list it here.
